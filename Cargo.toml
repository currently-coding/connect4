[package]
name = "connect4"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.9.0"
rand_chacha = "0.9.0"
nohash-hasher = "0.2.0"

[profile.dev]
opt-level = 3 # slight optimizations

[profile.release]
opt-level = 3 # maximum optimizations
